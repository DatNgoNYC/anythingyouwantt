import { ReactNode } from 'react';
import styles from '../style/Home.module.css'

const Searchbar = (): ReactNode => {
  return <div className={styles.searchBar}>Searchbar</div>;
};

export {Searchbar}